<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Finance Tracker"
            android:textSize="12sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"/>

        <ImageButton
            android:id="@+id/widget_refresh"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_popup_sync"
            android:tint="#FFFFFF"
            android:contentDescription="Refresh"
            android:scaleType="fitCenter"/>

    </LinearLayout>

    <TextView
        android:id="@+id/widget_balance_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Current Balance"
        android:textSize="10sp"
        android:textColor="#B0B0B0"
        android:paddingTop="4dp"/>

    <TextView
        android:id="@+id/widget_balance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="₹0.00"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:paddingBottom="12dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Quick Add"
        android:textSize="10sp"
        android:textColor="#B0B0B0"
        android:paddingBottom="6dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="2">

        <Button
            android:id="@+id/widget_add_income"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="↗️ Income"
            android:textColor="#FFFFFF"
            android:background="@drawable/widget_income_button"
            android:textAllCaps="false"
            android:textSize="12sp"/>

        <Button
            android:id="@+id/widget_add_expense"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="↙️ Expense"
            android:textColor="#FFFFFF"
            android:background="@drawable/widget_expense_button"
            android:textAllCaps="false"
            android:textSize="12sp"/>

    </LinearLayout>

</LinearLayout>
